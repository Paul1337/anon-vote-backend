databaseChangeLog:
  - changeSet:
      id: add-table-poll_tag
      author: paul
      changes:
        - sql:
            sql: CREATE TABLE poll_tag (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name VARCHAR(100) NOT NULL);
