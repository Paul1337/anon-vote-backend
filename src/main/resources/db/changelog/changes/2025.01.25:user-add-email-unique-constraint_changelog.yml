databaseChangeLog:
  - changeSet:
      id: user-add-email-unique-constraint
      author: paul
      changes:
        - sql: |
            ALTER TABLE users
            ADD CONSTRAINT un_users_email UNIQUE (email);

